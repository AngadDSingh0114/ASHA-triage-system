name: triage_engine
description: WHO IMCI / IMNCI deterministic triage decision engine for ASHA/PHC workers. Dart port.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
